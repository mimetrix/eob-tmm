# Self-review findings — what an adversarial read of this proposal turns up

### A structured self-critique by the author, not an external review. Organised by disposition.

**Status:** Self-review register · dispositions tracked here, fixes land in the docs they affect
**Origin, stated plainly so nothing here is over-read:** this is the author's own adversarial review of the author's own proposal, run as three passes over different parts of it (strategy docs · engineering scope · explainers and catalogs) and written from the standpoint of a long-time TMM developer. **No F5 engineer has reviewed this proposal.** Nothing below carries anyone's endorsement, and where it reads as a verdict, read it as an argument the author could not defeat rather than one anybody else has weighed in on. The value of the exercise is what it found, not who found it — and the findings that survive independently of judgement are the ones checked against source. The third checked the claims the design depends on against the **uBPF and PREVAIL sources cloned into the working tree** as `ubpf/` and `ebpf-verifier/` — so a number of findings are demonstrable rather than arguable. Those two trees are gitignored third-party clones, not vendored, so reproducing a citation means cloning them yourself; the commits the citations were checked against are uBPF `c900ed9faf1d41358a7ea9217ccd0b64a4ee8d5d` and PREVAIL `06769f7b508214e63b97905d275920f7e90182fa`. (The README documenting the clone step was in the removed prototype directory; the clones are untouched and every upstream-source citation below still resolves. Citations into the prototype's own code do not, and each is called out where it occurred rather than quietly dropped.)
**Companion:** [`engine-hard-problems.md`](engine-hard-problems.md) (problems we already knew about) · [`development-scope.md`](development-scope.md) / [`development-scope-code.md`](development-scope-code.md) (what the effort numbers apply to)

---

## Where this read lands, stated first

The conclusion of all three passes is the same: **two questions settle on paper (§7) and three are worth proving in a lab (§8) before the twelve-item build is designed further.** The reasoning:

- **The mechanism's shape survived all three passes** — patchable entry pad, trampoline, signed per-build hook map, monitor-before-enforce, signing gate as the perimeter.
- **The `ctx` model as written does not work**, and that is provable from PREVAIL's own source, not a matter of taste.
- **The scope was understated, item by item** — and deliberately not restated as a multiple, because the scope docs' original sizings were *size classes* ("small", "40 lines", "≈ a page per arch", "hundreds of lines") and there is no honest baseline to multiply. What §5 says instead is which items are understated and why, ranked. **Two items were not on the list at all** — the safe point, and debuggability through the trampoline. That absence is the finding, not a multiple.
- The reframe: *"the subsystem you are adding is not the VM — it is a code-patching, live-text, dynamic-code-loading facility in the crown-jewel process, with its own build-pipeline toolchain and a permanent per-build ABI."* That is the scope the items have to be sized against; sizing them smaller defers the cost rather than removing it.

---

## 1 · Demonstrably broken — verified against source or empirically

### 1.1 Fixed already

O9 and O10 were filed under §1.2 and have since been resolved; they keep their original labels here so citations elsewhere in this register still resolve.

| # | Finding | Fix |
|---|---|---|
| F1 | **`substrate/hook-point-map.json` omitted the `mode` field of `struct ls_ctx`.** Real offsets are `opcode 0, payload_len 2, avail_len 4, mode 8, head 12`; any tool re-deriving offsets by packing the field list computed `head` at **8**. A shield reading `head[0..15]` would have read `mode`'s four bytes plus twelve of `head`. The ctx-versioning failure mode the design warns about, realised in the artifact the docs cite as validated — with every check green, because a hash of a wrong map is a wrong map. | Field restored; authoritative `field_offsets` + `ctx_size` added; **`make check-offsets`** generates `_Static_assert`s from the map's own offsets and compiles them against the real header. Verified in both directions. (`5f55d3a`) Unlike most of this register, **this row is still reproducible in-repo**: the map and the header it is checked against ([`substrate/example_hook_ctx.h`](substrate/example_hook_ctx.h), where the struct now lives explicitly as an example ctx rather than a prototype structure) both survive, and `check_offsets.py` still fails the build if the `mode` field is removed again. |
| F2 | **The shield program chased two levels of TMM heap pointer.** `src/crab/ebpf_transformer.cpp:488-492` — a load from `ctx` yields `T_NUM`, an unconstrained number; dereferencing `T_NUM` is rejected unconditionally, and **a NULL check cannot promote a number to a pointer**. So the worked example was rejected *with or without* its guards, and the claim that "PREVAIL requires a NULL-check before every dereference" inverted what actually happens. | The program sees **resolved scalars only**. The hook map declares a bounded pointer walk; the host's generated ctx-builder follows it in native C, NULL-checked per step. Validity argument: the walk is restricted to the chain the hooked function is *itself about to dereference*, so stale pointers would have faulted in the body regardless. (`3cb4b1f`) |
| F3 | **Safe-return eligibility was classified by return type alone**, so a `void` function went straight to enforce-capable. `void` means no return value to fake, **not** no side effects to lose. | Two gates: skippability (side effects; closed by default; unanalysed = refused) then return kind. (`3cb4b1f`) |
| O9 | **The budget pass's decoder was wrong.** It read the whole **ELF file** as instructions (the first 8 bytes are `\x7fELF`), strode 8 bytes blindly so `lddw` (16-byte) mis-decoded into garbage instructions and garbage branch targets, and never treated `exit` as a terminator. Found by writing the sketch out as real code. (The sketch is still printed in `development-scope-code.md`, in item 8's retired-sketch block, kept for contrast — cited by section rather than line number, which had drifted.) | Rewritten as [`substrate/budget_pass.py`](substrate/budget_pass.py): parses the ELF, decodes `.text` only, handles `lddw`'s 16-byte form, treats `exit` as a block terminator, builds the CFG and prices the longest path. Verified running under `make -C substrate check` — now against the pass's **own six-case self-test** (hand-assembled eBPF in a synthesized ELF) rather than the three clang-built shield objects it originally priced, which lived in the removed prototype and are gone. That substitution *strengthens* this row: none of those three objects contained a `lddw`, so the 16-byte-form bug in this finding was never actually covered by the thing the fix was verified against. It is covered now. (`d68c994`) |
| O10 | **`prevail_loop_bounds()` cannot be implemented.** PREVAIL exposes **one aggregate number** (`max_loop_count`, the max over all loops — `src/result.hpp:146`, `src/fwd_analyzer.cpp:133-138`), not a per-loop-header map, so there is nothing sound to price a loop with; the only sound bound would be `total_instructions × max_loop_count`, useless at the 100k default. | **Resolved by refusal**, not by a bound: [`substrate/budget_pass.py`](substrate/budget_pass.py) refuses any program with a loop back-edge and says why, citing the aggregate. A coarser bound was the alternative; refusing states the limit instead of disguising it, and it costs nothing at day one because no predicate written down anywhere in this repo loops. The refusal is now an asserted case in the pass's self-test — which is the first time it has been exercised at all, since none of the deleted prototype's shields contained a loop either. (`d68c994`) |

### 1.2 Open — each changes the design

| # | Finding | Source | Disposition |
|---|---|---|---|
| O1 | **PREVAIL permits `ctx` writes.** The descriptor is four ints (`size/data/end/meta`); `writable: []` is not something PREVAIL consumes, and with no pointer slots *every byte of ctx is writable by a verified program*. Compose with "the ctx **is** the saved register frame" + "restore registers on fall-through" and a signed, verified, nominally read-only base-tier program becomes an **argument-injection primitive into live TMM code paths** — delivered by the safety mechanism. | `src/spec/ebpf_base.h:70-75`, `src/crab/ebpf_checker.cpp:362-364` | **ctx must be a copy** into per-core scratch, discarded on fall-through. Stop claiming read-only enforcement we cannot express. Cheap fix, mandatory. |
| O2 | **`--program-type` does not exist.** Program type is deduced from the ELF **section-name prefix** against a compiled-in C++ table; the fallback is `socket_filter`. Registering a TMM program type is a PREVAIL patch set — a fork by any name, with a rebase cost per release. | `src/main.cpp:59-148`, `src/linux/linux_platform.cpp:172-200` | Either own the patch set as a scoped item, or **reuse PREVAIL's existing `tracing` type** (12 u64 args, nothing dereferenced) and change nothing. The latter is free and forces O5 to resolve honestly. |
| O3 | **A green verify gate from stock PREVAIL can be near-worthless evidence.** Given no program type — and there is no `--program-type` to give it (O2) — PREVAIL falls back to `socket_filter`, whose descriptor *is* `__sk_buff`: 192 bytes, with pointer slots at 76/80/140. Any ctx struct smaller than 192 bytes therefore verifies clean while missing the pointer slots entirely. That demonstrates a small struct fitting inside a big one, not that a TMM ctx model verifies. | `src/linux/linux_platform.cpp:198`, `src/linux/gpl/spec_type_descriptors.hpp:17,37,81` | **Reframed, and still open — the demo this was filed against no longer exists.** As filed, the row was about the prototype's verify-gate track: a 28-byte `ls_ctx` passing under the fallback, with the prototype's own README and TOOLCHAIN docs half-updated to admit it. That prototype has been removed, so **there is now no verify gate in this repo at all** — the evidence position is worse than when the finding was written, not better. The finding outlives its subject because the limit belongs to PREVAIL rather than to any one harness, and it will constrain the next demonstration exactly as it constrained the last. Bar for a replacement: verify against a real ctx model — PREVAIL's existing `tracing` type (O2) or a registered one — because a green run under the fallback is not evidence of anything worth crediting. |
| O4 | **`--termination` is off by default** — `check_for_termination = false` — so a default PREVAIL run proves memory safety, **not** termination, while hard-problems §1 rests on "PREVAIL proves halting." When enabled, the bound is a **loop-iteration ceiling — 100,000 iterations by PREVAIL's own default — not a time budget**. | `src/config.hpp:57`, `src/ir/syntax.hpp:465` | Any verify gate must pass `--termination` explicitly. The prototype's gate did not, and its docs had been corrected to say so rather than imply otherwise; **both the gate and those docs are now gone**, so the in-repo half of this row has no subject left and it reduces to a standing requirement on the gate F5 builds. State the 100k ceiling in `engine-hard-problems.md` §1 as what it is — an iteration count, not a duration — **still open**: §1 still calls the budget pass "WCET-*lite*" (worst-case execution time; O11). That is the argument for the budget pass: a program can be provably terminating and still run far past a hot hook's budget. |
| O5 | **Base tier vs. the worked example.** Reaching a pointed-to struct needs `bpf_probe_read` — a helper. "No helpers" and "this worked example" cannot both be true. | — | Decide in writing: (i) ctx is scalar argument values only (fentry model) and the CVE example is a scalar predicate — **now done**, F2 — or (ii) day one includes a bounded fault-tolerant `probe_read` helper, with the §4 surface it brings. Every real fentry-style shield eventually needs (ii). |
| O6 | **Fuel does not work under JIT.** `ubpf_set_instruction_limit` — *"It has no effect on JIT'd programs."* And a wall-clock deadline is **unmeasurable at hot-hook granularity on aarch64** (`CNTVCT_EL0` at tens of MHz → 10–40ns tick against a budget of tens of ns), so an earlier framing had the two layers backwards: fuel is the mechanism, wall-clock is reporting. Known corner: the instruction limit *does* work in the **interpreter**, so an interpreter-only high-assurance build has enforceable fuel today with no fork. | `vm/inc/ubpf.h:769-771` | **`engine-hard-problems.md` §1 has conceded this** and now states it the right way round, so nothing there needs changing. What remains open is (a) `development-scope.md` §4 and `development-scope-code.md` item 15, which still carry the earlier draft's framing and file the watchdog as optional for cold-path sites, and (b) the trilemma decision itself (§7): patch uBPF's JIT for back-edge fuel, own it, upstream it, move item 15 to day one — at the cost of "reused (uBPF with an F5 patch; see substrate/ubpf-patches/)" on the second of three components. |
| O7 | **`ubpf_compile`'s prologue does `sub rsp, 4096` unconditionally, with no stack probe** — a 4KiB frame at arbitrary depth in TMM's call graph, able to step clean over a single guard page. | `vm/inc/ubpf.h:46,53`, `vm/ubpf_jit_x86_64.c:1744-1746` | Use `ubpf_compile_ex` / `ubpf_jit_ex_fn` with a per-core preallocated program stack. |
| O8 | **The loader message is not authenticated except on LOAD.** `shield_msg` has nowhere to put the binding, so `shield_binding_of()` cannot exist. `prog_len` is attacker-influenced and read **before** authentication. `SET_MODE`/`STATUS`/`REVOKE` are unauthenticated at the TMM boundary, and with no nonce or epoch a captured `LOAD` replays after a `REVOKE` — **the kill switch is defeatable by replay**. | the ABI header and loader sketch as they stood when this was filed | **Landed in the artifact, and the citation now resolves to the fix rather than the finding.** [`substrate/shield_abi.h`](substrate/shield_abi.h) carries `binding` inside `struct shield_msg` at offset 16 with every offset pinned by `_Static_assert`; `shield_binding_of()` is a real accessor; `epoch` is a top-level field and the signature covers `op · epoch · mode · prog_len · binding · prog`. `development-scope-code.md` item 10's sketch validates `prog_len` against the datagram length before hashing and rejects a non-advancing epoch. What is **still open** is that item's own three-item TODO list — the received length is not in the header's declarations, `SHIELD_ERR_REPLAY` has no code in `enum shield_err`, and `shield_jit_fn` is typed for the two-argument JIT (O7) — plus one the header now flags against itself: `SHIELD_BINDING_WIRE_MAX` is hard-coded at 128, which is exactly 16 (preamble) + 112 (binding), so one added field in either overflows a buffer sized by it. It should be derived, not written down. |
| O14 | **The verifier and the runtime identify a program by different names, and nothing relates them.** PREVAIL selects the program to prove by **ELF section name** (`fentry/<hook>`); uBPF's loader selects the program to run by **function symbol name** (`strcmp(rf.name, main_function_name)`, where `rf.name` comes from the symbol table) — while uBPF's own header calls that parameter `main_section_name`, which is how the mistake gets made. So "PREVAIL verified this object" and "uBPF is running this program" are assertions about **two different identities**. In an object carrying more than one function, one program can be verified and a different one loaded, with no component in the chain positioned to notice: the signature covers the bytes, the verifier covers a section, the loader resolves a symbol. | `ubpf/vm/ubpf_loader.c:271` and `:294`; `ubpf/vm/inc/ubpf.h` (the `main_section_name` parameter name); PREVAIL `src/main.cpp` section-prefix dispatch | **Fixed in the artifact.** [`substrate/ls_vm.h`](substrate/ls_vm.h) now requires **both** names, and `ls_vm_arm()` reads the object's own symbol table to confirm the named function is defined **in** the named section before loading — refusing otherwise, and refusing on anything it cannot positively establish (undefined symbol, absent symbol, data symbol of the same name, truncated or malformed object). Every offset is bounds-checked against `elf_len`, which is attacker-influenced on the real loader path. Exercised against the good case plus wrong-section, absent-symbol and truncated-object cases. **The design consequence is bigger than the fix:** the hook map and the signed binding must carry *both* identities, because a binding that names only one does not pin what actually executes. |
| O13 | **uBPF's stack-usage contract documents the safe behaviour and implements the permissive one, in both directions.** `ubpf.h` states: *"If the callback returns 0 or there is no callback registered, the eBPF interpreter/JITer assume that the local function uses the maximum stack available according to the spec (512K)."* Neither holds. With no calculator registered the accessor returns `UBPF_EBPF_LOCAL_FUNCTION_STACK_SIZE` = **256**, not a maximum. And a calculator returning **0** is recorded as `UBPF_STACK_USAGE_CUSTOM` with value 0, so the function is given a **zero-byte frame** — the 16-byte alignment guard passes it (`0 % 16 == 0`) and nothing else rejects it, in the interpreter or in either JIT backend. A host that read the header and returned 0 to mean "assume the worst" would get the best case instead. | `ubpf/vm/inc/ubpf.h:374-392` (the claim), `ubpf/vm/ubpf_vm.c:2370-2404` (the behaviour), `ubpf/vm/ubpf_jit_x86_64.c:1827`, `ubpf/vm/ubpf_jit_arm64.c:1260` | **Resolves item 6a better than reconciling two constants.** [`substrate/vm_stack_policy.h`](substrate/vm_stack_policy.h) registers a calculator that returns exactly the bound PREVAIL was given, so runtime allocation and proof agree by construction rather than by coincidence — and it never returns 0, refusing to the conservative maximum instead. Nine assertions in `make check-stack` pin that. Worth an upstream issue: the header and the code should be made to agree, and a 0 return should be rejected rather than honoured. |
| O12 | **The binding commits to a TMOS version range, but the hookable set is a property of a component version *set*.** TMM's binary is assembled from ~two dozen independently versioned components (`input-manifest.yml`) plus vendored third party, and roughly half its functions come from builds that never saw the TMM build's flags — measured **48.9%** entry-padding reach on BNK/x86-64, 82–97% inside the TMM tree and **0%** across the component builds. So whether a hook exists, and what its `ctx` layout is, depend on *this build of this component*. `struct shield_binding` carries `build_min`/`build_max` — one `uint32` TMOS range — which cannot express "TMOS 10.207 **with** `crypto-0.2.11`". A shield targeting a function inside a component can therefore be admitted against a build where that component moved underneath a fixed TMOS version, with its `ctx` offsets silently stale. | measured 2026-08-12, [`env/tmm-build-environment.md`](env/tmm-build-environment.md); `input-manifest.yml`; [`substrate/shield_abi.h`](substrate/shield_abi.h) offsets 96/100 | **Bind to the hook map's identity rather than to a version number.** Item 5 already generates and signs the hookable set per build; adding its digest to the binding makes a shield commit to *that artifact* — which transitively covers the manifest that produced it — instead of to a range that cannot represent the thing being ranged over. Keep `build_min`/`build_max` as a coarse human-readable guard, and let the digest be the one that gates admission. Cheap: one fixed-width field, and the artifact it names already exists in the design. |
| O11 | **The budget pass is still described as a bound, and its retired numbers still appear.** The tool's own docstring now gets this right ("not a WCET bound … a *relative* sanity check"). It used to make the point with numbers too — cycle counts across three clang-built shield objects — but **those objects were in the removed prototype, so the measurement is no longer reproducible here and the numbers no longer have evidence behind them in this repo**. A per-op cycle table also under-counts memory (one L3 miss is hundreds of cycles), so calling the result a bound is unsupportable wherever it survives. | `development-scope-code.md`, item 8's preamble ("the layer that answers 'termination is not a time bound'"), `engine-hard-problems.md` §1 ("WCET-*lite*") — `big-ip-live-shield-design.md` §11 has since dropped the retired cycle figures and now says the cost table is uncalibrated | Strike "WCET-lite", drop the unreproducible cycle figures, and stop framing the pass as the answer to the time-bound question. It is an admission-time **relative** sanity check — say so in the headline, not the footnote. The stale worked number in the illustrative sketch (652 instructions → "800 cycles", 1.23 cycles/instr against its own `load: 4`) belongs to the sketch block and is tracked with it. |

---

## 2 · Wrong about TMM / BIG-IP — cheap to fix, and each one makes the argument stronger

These are the sentences that lose a technical room. Every fix below is *more* compelling than what it replaces.

The **Disposition** column was established by grepping the repo for each claim as written, not by trusting the pass that filed it: **fourteen fixed, three partly fixed, none fully open.** A partly-fixed row means the corrected sentence landed somewhere and the original survives somewhere else — which is the failure mode this register exists to catch, so the surviving location is named.

| # | Claim as written | Reality | Better claim | Disposition |
|---|---|---|---|---|
| T1 | "Standard capture is **blind** to TMM's fast path… the only viable tap is in-process" | `tcpdump -i 0.0` works today; `:p`/`:n`/`:0.0` give pre/post-TMM views; `tcpdump --f5 ssl` exports session secrets. TMM already implements a capture path. | "Existing capture is a packet tap at the interface boundary. What it cannot give you is **post-parse, post-decrypt state at an arbitrary internal hook**." The `tmmdump` pitch survives and gets more interesting. | **Fixed** — `tmm-usdt-tracepoints.md` §1 and §10.6 now open on BIG-IP's own capture path (`tcpdump -i 0.0`, the `:p`/`:n`/`:0.0` views, `--f5 ssl` key export) and claim only post-parse/post-decrypt state at an internal hook. |
| T2 | "would have taken TMM down **until the next maintenance window**" | `sod` restarts TMM in seconds; HA fails over. | "A **remotely triggerable, repeatable crash-loop** — every trigger drops every flow on that TMM and can flap HA." Worse, and true. | **Fixed** — `cve-shield-walkthrough.html`'s crash step now carries the better claim almost verbatim (`sod` restarts it in seconds; the harm is a repeatable crash-loop that can flap HA). |
| T3 | "every count — **a crash that didn't happen**" | A NULL deref kills the process once, then restarts. 251 fires ≠ 251 prevented crashes. | "every count is a **flow that reached the faulting path**" + a line on CMP (clustered multiprocessing) and its DAG (disaggregator) explaining uneven per-TMM distribution. | **Fixed** — the phrase survives nowhere in the repo but this register. |
| T4 | `CVE-2026-22548` presented in body text as a February 2026 advisory | Fabricated; the disclaimer is a footnote above it. SIRT will look it up. | Use a **real, published, closed** F5 advisory and work the example against its actual patch diff. Doubles as the shieldability study. | **Partly** — the fabricated ID is gone repo-wide and the walkthrough now says only "a real crash in TMM's data path." The substantive ask (a real published *closed* advisory, worked against its actual patch diff) is **open** and rides the §7 shieldability study. |
| T5 | "`bd` is a process we own and have symbols for" → easiest Phase 1 | `bd` is **multi-threaded C++**: no poll loop, no safe point, mangled names, references and by-value structs in signatures, RAII destructors a skipped body silently doesn't run. | Phase 1 belongs on a **designed-in call site on a cold TMM path** — which is also what actually proves the in-TMM spine. | **Fixed** — `big-ip-live-shield-design.md` §12 now leads with "**Not `bd`, despite the temptation**" and puts Phase 1 on a designed-in cold TMM call site, for the reason given here. `tmm-usdt-tracepoints.md` §6 and §11 both now say `bd` is explicitly *not* the Phase 1 target; the `tmm:bd:request_eval` hook that carried the old label no longer exists in the catalog. |
| T6 | "Enforcement plugins (`bd`/WAF, APM, AFM, DoS)… plugin-process internals" | **AFM and DoS enforcement are in TMM**; APM is split; `bd` is the separate one. | Split the row. Lumping them makes the scope look 4× smaller than it is. | **Partly** — split correctly in `tmm-usdt-tracepoints.md` §6.1 ("In-TMM enforcement filters (AFM, DoS, the TMM side of APM)"). Now also split in `embedded-ebpf-substrate.md` §4 into an in-TMM filter row (AFM, DoS, TMM-side APM) and an out-of-process `bd` boundary row that states the asymmetry outright. **The residue is the phrase itself**: re-grepped, "enforcement plugins" as a collective noun survives in one place — `big-ip-live-shield-design.md` §2.1, in the sentence describing where the iRule coverage holes cluster. |
| T7 | "the kernel is **structurally blind** to TMM" | Kernel eBPF *can* attach to TMM — uprobes/USDT on `tmm` work today. | "Kernel eBPF can attach but **cannot afford to** (per-hit kernel transition inside a run-to-completion loop) and **cannot enforce** (no `bpf_override_return` on uprobes)." Same conclusion, survives someone who has run `bpftrace` on `tmm`. | **Fixed** — `big-ip-live-shield-design.md` §2 and `README.md` both now argue reach-yes / afford-no / enforce-no. |
| T8 | iRules "TCL, **runtime-bounded**" | A runaway rule spinning in the TCL VM is a documented cause of a stalled TMM and a watchdog restart. And we reject WASM's fuel kill as "not a proof" two pages later. | "Unbounded in practice; a runaway rule is a documented TMM-restart cause." That is *our* argument, not our competitor's. | **Fixed** — `README.md` and `embedded-ebpf-substrate.md` §1's spectrum table both now read "TCL, **unbounded in practice** — a runaway rule is a documented cause of a stalled TMM and a watchdog restart." |
| T9 | Control plane = native daemons + JVM | Three runtimes: native, **JVM** (Tomcat, `restjavad`/`icrd`), **Node** (`restnoded`/iControl LX). `tmsh` is a per-invocation shell, not a daemon. | Runtime inventory with CVE count per runtime; drop `tmsh` as a shield target. | **Partly** — `big-ip-live-shield-design.md` §2 names three runtimes and demotes `tmsh` to a per-invocation shell; §5.1 drops it as a shield target in as many words. The **CVE count per runtime** was never built, so the prioritisation it was meant to drive is still unevidenced. |
| T10 | "The signal leaves the box; the data never does — and that's **provable**" | PREVAIL proves memory-safety over declared regions. It proves nothing about whether a returned scalar encodes payload. | Three mechanisms, none called a proof: host-owned schema-checked sink, declared window, signed extractor review. | **Fixed** — `data-plane-intelligence.md` §2 now names three mechanisms and calls none of them a proof; the pull-quote reads "held by mechanism and review, not by a theorem." "The only compliant way" is also gone. |
| T11 | TMM's blindness = "**none**"; sees "**every** flow, decrypted" | Offloaded flows, FastL4, SSL pass-through, client-SSL-only virtuals, non-terminated UDP/QUIC, per-box scope. | Delete "none"; state the real bounds; note it is *still* the least-blind vantage. | **Fixed** — the blindness table enumerates offload, FastL4, pass-through, client-SSL-only, non-terminated UDP/QUIC and per-instance CMP scope, and claims only *least-blind of the four*. |
| T12 | "no rebuild, no reboot, **no release train**" (hero) | The mechanism itself rides a release train; the walkthrough says so honestly, the hero doesn't. | Add "once the enabling build ships." One clause. | **Fixed** — `programmable-dataplane-engine.html`'s hero adds the clause: "*(The engine ships in one enabling build; …)*". |
| T13 | "retarget a program from a TMM parser to an httpd request path by changing only the hook name" | Different ctx, different verifier, different helpers. It wouldn't compile. | Cut the clause; keep "one DSL, two engines." | **Fixed** — `programmable-dataplane-engine.html` now says "retargeting is a **re-author, not a rename**." |
| T14 | "an unarmed function boundary is a nop pad — **free**" | Pads cost i-cache and i-TLB footprint on an i-cache-sensitive process, unconditionally, for every customer forever. | "Free at *runtime*; the pads have a **build-time footprint cost** we have measured" — see §4. | **Fixed** — `tmm-usdt-tracepoints.md` §1, `big-ip-live-shield-design.md` §11 and `programmable-dataplane-engine.html` all now say free at *runtime* with an unconditional build-time footprint — and name the measurement as the feasibility phase's first deliverable rather than asserting it is already measured. |
| T15 | Memory pools / poll-loop stats have "**no** surface at all" | All in `tmctl`/`tmstat` and every qkview. | "Already counted, but not **conditional** — you can read the aggregate, you can't say 'record the last 200 samples leading into the stall.'" True and better. | **Fixed** — `tmm-usdt-tracepoints.md` §9 carries the aggregate-vs-conditional form verbatim, `tmctl`/`tmstat` and qkview named. |
| T16 | "The exposure window is a solved problem everywhere the kernel can reach" | No industry practice of eBPF-shielding kernel CVEs in production. | Name the real precedent (ftrace/livepatch, error injection) and claim the *idea* is established, not the practice. | **Fixed** — `cve-mitigation.html` and `big-ip-live-shield-design.md` §13 both claim the *idea* is established (kernel livepatching, ftrace-based error injection) and that the practice has never reached a proxy data plane. |
| T17 | "Block a live exploit **in hours**" | Author → verify → budget → **HSM release signing** → distribute → monitor → enforce. Signing is not hours-scale today. | "**Days** — and the process change that would compress it: a pre-authorised shield-signing path so it can be hours." | **Fixed** — `big-ip-live-shield-design.md` §8 and `cve-mitigation.html`'s headline now say "**days, not hours**" and attach the pre-authorised-signing ask, and the two "in hours" promises that survived in `embedded-ebpf-substrate.md` are gone. |

---

## 3 · Missing specifications

**The item that isn't on the list, and is the most expensive one.**

- **Item 0 — the safe point itself.** Every in-TMM item is predicated on "a safe point between poll-loop iterations that dequeues and processes `shield_msg`." It does not exist in TMM today. Building it means a per-instance message queue reachable from the config channel, **a new check in the poll loop** (one load + branch per iteration, on the loop this org has rejected proposals for touching), and a bounded work budget for the handler — because as sketched, `do_load` performs `ubpf_load_elf` + `ubpf_compile` *at the safe point*, i.e. a full ELF (Executable and Linkable Format) parse and a JIT compile inline in the poll loop. For however long that takes the loop is not polling: a latency spike, and possibly a dropped heartbeat / HA (high-availability) event. **Move compile and page population off the safe point**; leave it publishing a pointer and patching a few bytes.

**Mechanism gaps.**

- **Live text.** *Partly closed.* `development-scope-code.md` now carries `tmm_text_make_writable()`, the matching re-protect calls, and the concession that "whether W^X can be relaxed per-page in TMM's memory manager at all is a **TMA question**" — so the mechanism is named and its riskiest assumption is on the TMA list. The residue is still open and still unanswered: is TMM's text shared or private across instances (shared ⇒ patching one patches all, and there is no cross-instance safe point; private file-backed ⇒ **COW (copy-on-write) of the whole page per instance**, 2MiB per patched page if hugepage-backed, and the iTLB win is gone); SELinux `execmem`/`execmod` policy on a hardened appliance; **code-integrity / FIPS self-test / image-hash mismatch the moment a shield arms**; and re-protect return values currently dropped, so an error path leaves TMM text permanently writable.
- **x86-64 patching is not atomic.** A 5-byte `JMP rel32` cannot be written by one store; this is why Linux has `text_poke_bp()` (int3 → IPI/sync → patch → sync → replace first byte). "Atomic patch + i-cache flush" cites the conclusion and skips the mechanism. aarch64 *is* a single aligned 4-byte store — but needs per-core cache maintenance + `ISB`, and an 8-byte pad **cannot hold an arbitrary-range branch**, so the trampoline must be linked in `B` range as a build-checked constraint (the "veneer" answer is unanswerable as written, because nothing reserves in-range space).
- **Register save is under-specified in ways that corrupt or crash.** Missing: **FP/SIMD** (`xmm0-7`/`v0-v7` — a hooked function taking a `double`, and `tramp_dispatch` is C); **`rax`** (varargs vector count — and the worked example hooks a *log* function); **`x8`** (indirect result location); `r10`, `x18`; **stack-passed arguments**, which a "ctx over saved registers" cannot see at all; and **CET-IBT / BTI** — jumping to `entry + pad_bytes` is an indirect branch into mid-function with no `ENDBR64`/`BTI` landing pad, which is an immediate fault on hardened builds. Also **CFI/`.eh_frame`** on the stub, or every backtrace through a hooked function is garbage.
- **"Execute the instructions the pad displaced"** — nothing is displaced; the pad is nops. Delete it; the deletion makes the design simpler.
- **Unload while executing.** Disarm and `ubpf_destroy` in consecutive statements, and `slot->armed/mode/fn` are plain non-atomic fields read on the hot path. Needs an **epoch / quiescence** scheme (disarm, advance per-core epoch at each safe point, free when all cores have passed) — a standard pattern, and its absence is the difference between "delicate" and "will eventually crash a customer."
- **Re-entrancy.** No guard in `tramp_dispatch`. Arm a shield on a logging function and the loader's own error path (`log_warn`) re-enters it. The worked example hooks a log function. **And the mutual case is worse than the self case:** hook A's generated ctx-builder calls a function carrying hook B, whose builder calls something carrying hook A. One per-core depth guard covers both (`engine-hard-problems.md` §3.1).
- **More than one program armed at once was unaddressed entirely** — now `engine-hard-problems.md` §3.1, as five consequences of a six-row support table. The first was a defect rather than a policy gap and is **closed in the candidate ABI**: overwriting `slot->fn` would have let a second `LOAD` on an armed hook **silently replace a live shield** and reset its fire counter, making the swap look exactly like "no matches", so [`substrate/shield_abi.h`](substrate/shield_abi.h) now records one program per hook and `SHIELD_ERR_BUSY` as the required refusal, with replacement being `REVOKE` then `LOAD`. Day one is the loader enforcing what the header states. Then: outcome composition if chaining is ever wanted (load order becomes observable behaviour, and load order is config-sync arrival order, which is not the same on two HA peers); **no ceiling on cumulative armed cost**, where the per-hook framing quietly assumes independence and a flow actually pays the **sum** of the hooks it traverses; and **cross-hook masking** — if shield A safe-returns past a body containing hook B, B never fires and reads zero, which is indistinguishable from "nothing matched"; and **mutual re-entrancy** — hook A's generated ctx-builder calling a function that carries hook B, whose builder reaches back into A — which is the fifth, and is the case the per-core depth guard above exists for. Cross-hook masking is the same false-success shape as partial inlining, except caused by our own mitigation.
- **Inlining, LTO (link-time optimisation), ICF (identical code folding) and IPA (interprocedural analysis) clones — named, unfixed.** No longer unmentioned: `development-scope-code.md` covers it at length (partial inlining, `-fipa-icf` folds, `.constprop` clones, "the nearest surviving out-of-line ancestor", the skip radius, `noinline` as the only guarantee) and so does `cve-shield-walkthrough.html` build step 2. What has *not* changed is the artifact: **item 5's `hookable()` test still drops fully-inlined statics** (no `low_pc` to find) **and still mis-maps folds and clones** — at `-O2`, `-fipa-icf` folds identical functions so arming "A" arms "B" and name→address is not a function, while `ipa-cp`/`ipa-sra` produce `foo.constprop.0` clones with **different signatures** than the name they were derived from. So the finding is now a gap between the prose and the tool, which is the cheaper kind: **the hookable set is optimizer-determined**, guaranteeing a function is hookable means `noinline` — a *source change* that undercuts "no source modification" — and the generator must publish the resulting hookable set as a build artifact rather than answer name lookups.
- **`path_class` is a claim about traffic that a CVE shield systematically violates.** The worked example's "cold" hook is a log function on a malformed-input path — the path the attacker drives. At line rate it is the hottest code on the box, running an unbudgeted program with no watchdog. `path_class` must be **static structure ∧ adversarial reachability**. *Landed* — `tmm-usdt-tracepoints.md` §2 now defines the column that way and its tables give both readings as *structure* → **budgeted class** wherever they differ, escalating any hook an attacker can drive above its structural rate.
- **All-or-nothing arming across TMM instances** has one clause and no semantics. Partially armed = still crashable (the attacker's flow only has to hash to the unarmed instance), and identical traffic gets different treatment by disaggregation.

**Platform and lifecycle gaps.**

- **vCMP / F5OS tenants / chassis.** Named as a concern, absent from all seventeen items. Per guest: whose hook map, whose signing authority, whose audit trail, whose change window — and how a newly joining TMM/blade/guest is armed *before* it takes traffic.
- **HA.** Failover to a peer on a different build = failover **into** the unshielded vulnerability.
- **ISSU.** A new TMM starts with an empty shield set, so there is nothing to "expire"; the real problem is who re-verifies and re-pushes, and **the window where the new instance takes traffic with nothing armed**.
- **Core dumps / qkview / debuggability.** Zero coverage. Backtraces through the trampoline, PC inside an anonymous JIT mapping, on-disk text ≠ in-memory text (breaking symbolisation and integrity checks), armed-shield inventory in the dump and in qkview. Absent this, support escalates for a global disable after the first field crash.
- **uBPF's runtime hardening is per-VM and mostly off by default.** Read from
  `ubpf/vm/ubpf_vm.c` (`ubpf_create`): bounds check **on**, read-only bytecode **on**,
  undefined-behaviour check **off**, **constant blinding off**. Blinding is the JIT-spray mitigation —
  without it an immediate in the bytecode reaches the JIT'd buffer verbatim, so a program the verifier
  admits can still place native instruction bytes in an executable mapping inside TMM. The loader
  sketch went `ubpf_create()` → `ubpf_load_elf()` and inherited every default silently; it now sets all
  three explicitly. **And the asymmetry is worse than the default:** `ubpf.h` states blinding is
  *"ARM64: Not yet implemented — enabling on ARM64 will have no effect,"* so on aarch64 the mitigation
  does not exist at all. That is a TMA item and an argument for the interpreter on any high-assurance
  aarch64 build, alongside item 15's fuel — which is enforceable only in the interpreter for the same
  kind of reason.
- **41 tracepoints, no consumer.** The catalog defines 41 tracepoints in its tables with typed `ctx` fields and the scope
  list covers **transport** for their records (item 13's log line, item 14's ring and drain agent), but no
  item and no document specifies a **sink, a view, or a metric schema**. Both sinks are explicitly
  undecided in their own items. The two named consumers, `tmmtrace` and `tmmdump`, are placeholder names
  for unbuilt utilities. So the day-one visible surface is item 11's `status` — mode, armed, epoch,
  per-core fire counters — which answers "is the shield working" and nothing more. **The asymmetry is the
  finding**: TMM's existing poll-loop and pool counters already reach `tmctl`/`tmstat` and every qkview
  (T15 above), and new tracepoints inherit none of that, so a signal with no sink is indistinguishable
  from one never collected. It lands hardest on the catalog's *primary* stated lens — observability,
  debug and RCA — while the shield use case, a peer consumer by that document's own framing, is the one
  with a console. Filed as [`development-scope.md`](development-scope.md) item 14a.
- **Memory accounting.** uBPF `malloc`s and `mmap(PROT_EXEC)`s; TMM has its own preconfigured allocator with strict accounting. `SHIELD_MAX_SHIELDS 64` has no memory model behind it.
- **Distribution.** Items 9–11 run sign → push → load, all in-box. Nothing covers F5 → customer fleet: versioned distribution, pinning, rollback. (The AWAF attack-signature channel is the obvious analog: signed, versioned, live, fast cadence, established customer trust — and it decouples mitigation cadence from hotfix cadence, which is the point of the exercise.)
- **Per-build shield multiplication.** A per-build `ctx` means **per-build shields**: one CVE across six supported branches is six verified, budgeted, signed, tested artifacts — with possibly different hookable sets. "A few lines of C per CVE" quietly becomes "a few lines × N builds."
- **Testing.** Not one of the seventeen items is a test item. Minimum bar: arm-every-hook soak under full traffic asserting zero delta; arm/disarm churn under load; hook-map offset round-trip gating the build (**now built** — `make check-offsets`); differential fuzzing of both `binding_serialize` implementations and of the pre-auth message parser; and an adversarial negative corpus for PREVAIL — which is also the verifier-auditability deliverable, at no extra cost.
- **Performance regression gating.** No baseline, no budget, no CI gate — for a data-plane feature. This is what a TMM review leads with.

---

## 4 · The measurement that comes first

All three passes landed on the same thing before anything else, for a reason that has
nothing to do with approval: **every affordability claim in this package is currently an estimate.** "Free
when dark," "one predictable branch," "tens of nanoseconds is noise" — none of it has been measured. The
flag is image-wide, it interacts with ICF and LTO, and it has no runtime opt-out, so until it is measured
the design cannot choose its own shape.

**Status — the static column is done; three of four remain.** On BNK/x86-64 the A/B build has been
run (method, numbers and caveats in [`env/tmm-build-environment.md`](env/tmm-build-environment.md)):
the flag compiles cleanly across 2,039 files under `-Wall -Werror`; `.text` grows **+0.476%** and the
binary **+0.759%**; alignment slack absorbs **21%** of the nominal pad width, which answers the
pad-placement question below; and **48.9%** of the shipped binary's functions were reached, because
roughly half of them are built by separate component builds rather than the TMM build — so the
*paddable* set is materially smaller than the hookable set, and closing the gap is a coordination
problem across component teams rather than a flag. Full coverage extrapolates to **~+0.97% `.text`**.
Throughput, latency tail, instruction-fetch behaviour and aarch64 are all still unmeasured.

**The experiment.** Build TMM twice from the same source — once normally, once with
`-fpatchable-function-entry` — and run both under identical traffic on at least one x86-64 and one
aarch64 platform, with **nothing armed** in either. Hold constant the source revision, optimisation level
and PGO (profile-guided optimisation) profile, platform, traffic mix, and connection and request rates.

Report the deltas for:

- **throughput** — packets/sec and connections/sec
- **latency distribution**, at the tail rather than the mean; a proxy is judged on p99/p99.9
- **text size**, plus whether `.text` is shared or private across TMM instances and whether it is mapped
  on huge pages
- **instruction-fetch behaviour** — i-cache MPKI (misses per thousand instructions) and i-TLB misses

Two structural questions the same build answers for free:

- **how many of the functions worth hooking survived as their own out-of-line symbols** rather than being
  inlined, folded by ICF, or cloned by IPA — this is the real hookable set, and it is the optimiser's
  decision rather than ours
- **whether pad placement matters** — nops emitted before the entry label can occupy alignment slack that
  already existed, so measure both placements rather than assuming the cost is the pad width

**Then a second measurement, cheap once that build exists.** Arm one hook, put a program on it, and
measure the same quantities again at rate. This is a different question: the first tells you what the
mechanism costs while idle, the second what it costs in use, and only the second bounds which hook rates
are available.

**What the result decides — the form of the mechanism, not whether to proceed.** Three outcomes, all of
them viable:

1. **The idle cost is in the noise.** Pad the image; the full function-boundary reach is available.
2. **It is measurable but concentrated.** Pad an allowlisted subset of translation units instead. Reach is
   narrower and fixed at build time, and most of the cost goes away.
3. **It is material even selectively.** Designed-in call sites only. Hooks are placed in source, so an
   unanticipated bug is out of reach and both the observability and CVE cases narrow to what was
   anticipated in advance.

The threshold separating those outcomes is not this document's to set. It belongs to whoever owns the
platform's published performance numbers, and it should be agreed **before** the experiment runs rather
than argued about afterwards.

---

## 5 · Scope, restated honestly

**Scope boundary, stated first: the proxy data plane.** Everything scoped below concerns **TMM**.
The control/management-plane daemons, and in the containerized form factor the companion
microservices deployed alongside the `f5-tmm` pod, come **later** — each is a separate engine and
verifier (kernel eBPF via uprobes; JVMTI or Node probes for the two managed runtimes), so folding them
into this scope would multiply the trust story before the first one is proven. What must still be
designed to span both from day one is the **lifecycle** — one signed catalog, one revocation path —
because retrofitting that is the expensive kind of mistake. See README, *The mechanism*.

**No effort figure is offered here, deliberately.** This is a design proposal, not a plan, and
converting these items into months or people is the job of whoever picks the work up — with their own
team, their own build system, and their own idea of "done". What this section *does* establish is the
**shape** of the work, because that is where the scope document was wrong in a way that matters: it
reads as a list of tools and small components, and item by item it is a subsystem.

The column that matters is the third one. "As scoped" is what `development-scope.md` claimed; the
verdict is how far off that reading is.

| Item | As scoped | Verdict | Why |
|---|---|---|---|
| **0 · safe point** | *not listed at all* | **Missing, and among the largest** | New poll-loop check, per-instance queue, moving the JIT off the safe point. It changes the one loop every safety and cost argument in the package rests on. |
| **0b · the flag** | *"build-system configuration, not code"* | **Badly understated** | §4. A whole-image rebuild, ICF interactions, hand-written asm and third-party translation units, and a full regression review. It is an experiment plus its fallout, not a configuration line. |
| 1 · trampoline | "≈ a page per arch" | **Badly understated, and it is per-architecture** | The full clobber set including FP/SIMD, CET-IBT/BTI, varargs and `x8`, CFI (call-frame information) so core dumps survive, the ctx copy, a re-entrancy guard. The assembly is the short part; the ABI edge cases are the long one. |
| 2 · arm/disarm | "small" | **Badly understated, and TMA-gated** | W^X relaxation in TMM's memory manager, shared-vs-private text, hugepage COW, code-integrity interaction, a per-arch `text_poke` equivalent. |
| 3 · loader handler | "hundreds of lines" | **Understated** | Missing epoch and quiescence handling, re-entrancy, memory accounting, `do_set_mode`, ISSU (in-service software upgrade). |
| 4 · signature verification | *"possibly reusable"* | **Was missing from this table entirely** | On-box verification of the signed binding against a baked-in key, plus the binding serialization and its failure paths. |
| 5 · hook-map generator | "tool" | **The sleeper — the most understated item here** | A **SysV/AAPCS parameter classifier** driven from DWARF: struct-by-value splitting, HFA/HVA, stack spill, unions — against an optimised build, per architecture. |
| 6 · ctx descriptors | "40 lines" | **Badly understated, and permanent** | O1/O2, DWARF-true offsets, a C++ platform-table entry in PREVAIL. The scope doc is right that this is the largest conceptual item and wrong about its size. |
| 7 · safe-return table | "tool + process" | **Understated** | `caller_null_checked` is whole-program call-site analysis, plus typedef and enum resolution, plus a human review process that never ends. |
| 8 · budget pass | "conventional… tool" | **Partly done, and the rest is real** | O9 is done and O10 is closed by refusing loops rather than bounding them (§1.1). What remains is O11's framing plus a calibrated per-target cost model. |
| 9–12 | "conventional" | **Fair as scoped** | Nothing in this read disputes these. |
| 15 · back-edge fuel | "optional, staged" | **Understated, and it is day one, not optional** | O6 and §3's `path_class` finding. It is a uBPF JIT patch on **both** backends, plus the plumbing, plus carrying the fork. |
| — · debuggability | *not listed* | **Missing** | Core dumps, qkview, and whether a backtrace survives the trampoline. |

**Read the table as a ranking, not a budget.** Four items — the safe point, the trampoline, arm/disarm,
and the hook-map generator — are each substantial engineering in their own right, and two of them were
not in the scope document at all. That is the finding. What it adds up to is a judgement for the people
who would do the work, and they will size it more accurately than this register can.

**Excluded from the table, deliberately:** items **13** (rate-limited per-firing log line), **14**
(egress ring + drain agent), **16** (canary auto-unload) and **17** (the authoring DSL, which
`development-scope.md` used to list as already-working prototype code and now correctly lists as
proposed and unbuilt). All four are real work and none of them is on the critical path to answering
the three questions in §8.

**Overstated (easier than presented — say so, it helps):** a designed-in **USDT catalog needs no VM at
all** (static markers plus a host reader gets most of the observability value), and auto-retirement is a
local version read plus a boot hook rather than an iControl-REST polling subsystem.

## 6 · What the reviews backed — and where it currently sits

Ordered by how far forward each was asked to move.

1. **"The signature, not the verifier, is the security perimeter."** It converts "you put a JIT in the crown jewel" from **traffic-borne RCE** — remote code execution triggered purely by sending traffic through the data path, needing no credentials and no management-plane access — into supply-chain/insider risk. It was on pages 3 and 4, and `cve-mitigation` had reduced it to "Signed by F5," bullet 2 of 4. **Done — the recommendation was taken:** `cve-mitigation.html` now states it in full — "The signature — not the verifier — is the security perimeter", with the RCE-to-supply-chain collapse spelled out. Nothing further to move.
2. **Signing the binding, not the bytes** — `prog_sha256 · hook · build_min..max · mode_ceiling · expires_with`. It closes replay and privilege escalation in the same field, `mode_ceiling` being the one that caps enforce.
3. **Signing last, conditional on the verify and budget reports**, with the report's hash cross-checked against the bytes. The signature *is* the attestation.
4. **The iRule reachability test** — does an event fire *before* the vulnerable code; is the condition observable in the iRule data model at that event; does the flow reach the iRule VM. It is the same reasoning a TMM engineer applies, and the ASCII coverage map states the answer per hole. Lead with this, not the programmability-spectrum table.
5. **"Monitor mode is not a safe soak" for crash-class shields** — the first true positive in production both confirms the predicate *and* crashes the box. A deployment consequence a demo framing hides.
6. **Refusing arbitrary override; declaring un-shieldable CVEs out of scope.** "Better to declare a CVE out of scope than ship a shield that returns into an inconsistent TMM."
7. **The entry-pad-at-+0 choice, whose best property is unstated:** patching *before* the prologue means `x30`/`[rsp]` still holds the caller's return address, so `SAFE_RETURN` is a bare `ret` — no frame to unwind, no epilogue to synthesise, no shadow stack to fix. **That is why this is tractable where a return-boundary hook would not be.** Say it.
8. **Entry-pad hooking catches every caller**, including indirect calls through function pointers and calls from hand-written asm.
9. **Operational discipline:** `load` refuses `enforce`; promotion is separately authorised with a stricter role; a warning when promoting a shield that has never fired; `fired[]` deliberately not cleared on disarm ("the evidence outlives the shield"); all-or-nothing fan-out with unwind.
10. **Item 7's two refusals** — a status-code return where 0 means success is not safe-returnable, and unannotated defaults to observe-only — plus the `rationale` field recording what is lost per function.
11. **Fail-dark everywhere, eleven distinct rejection codes.** The error taxonomy *is* the item.
12. **Offload and form-factor honesty** — naming the coverage gradient before someone else does, and noting it applies equally to iRules and kernel eBPF.
13. **The licensing analysis** — the GPLv2-kernel-verifier counterfactual as the *reason* permissive PREVAIL matters, plus PPL/APRON/ELINA copyleft risk and an SBOM (software bill of materials) gate.
14. **Observe-mode diagnostics with governance as part of the mechanism** — context minimisation, residency, time-boxing, tamper-evident log — rather than as an overlay on it.
15. **A working demonstration whose verify gate rejects a deliberately unsafe program.** Listed here as an *opportunity, not an asset*: **no such demonstration exists in this repo.** One approximating it did, and has been removed; per O3 its gate was running under PREVAIL's `socket_filter` fallback, so it would not have survived the first question anyone asks, which is "against which ctx model?". Build the version that verifies against a real ctx model, then show it in the room before any slide.
16. **The "claim to retire" device** — naming *termination ≠ WCET*, *the ctx ABI is the actual project*, *maps don't work like Linux*. Each states a limit before someone else has to find it.
17. **The verifier-auditability ask** — and it is nearly free: PREVAIL already ships `-v`, `--failure-slice`, `--asm`, `--dot`, `--line-info`. The soundness-evidence package can be built on existing flags.
18. **Real, checkable artifacts** in [`substrate/`](substrate/): `shield_abi.h` compiles with every assert holding, the schema validates the map, `check_sr_gates.c` and `check_offsets.py` fail the build on regression, and the budget pass passes its own self-test — all under one `make -C substrate check`. **The reason F1 was findable at all is that these files exist.** Since the prototype's removal this is also the *only* executable evidence in the repo, which raises what it carries: it substantiates the ABI and the admission-time checks, and it substantiates nothing about loading, arming, or running a shield.

---

## 7 · Two things to settle on paper, before any build

These need no engineering at all — no build, no lab, no measurement rig — which is why they belong
before §8's three rather than alongside them. Either one can change the shape of the programme, and the
first can change whether there is one.

1. **The retrospective shieldability study.** Take the last three years of *real* F5 data-plane advisories — every TMM and `bd` CVE. Per CVE: was there a reachable boundary exposing the triggering condition in its arguments; which named function; what the safe outcome was; would the predicate have had an acceptable false-positive rate. Report **N shieldable, M not, and the M-list reasons**. This is `big-ip-live-shield-design.md` §1.1's assumption 8, which that document marks as **UNKNOWN rather than merely unproven** and as ending the case if false: nobody has taken a set of published F5 data-plane advisories and checked them one by one. A focused SIRT-plus-engineering exercise settles it. (If the answer is "3 of 40, all in `bd`," that is a different and smaller project.)
2. **The trilemma — pick one, in the room, not in month nine.** With no preemption, enforcing a time bound needs fuel; fuel has no effect under uBPF's JIT; wall-clock is unmeasurable at hot-hook granularity on aarch64. **Which do you give up: the run-to-completion loop, the unmodified uBPF, or enforce mode?** The available good answer — fork uBPF's JIT for back-edge fuel, own it, upstream it — costs "reused (uBPF with an F5 patch; see substrate/ubpf-patches/)" on one of the three reused components (uBPF now with an F5 patch).

**And the question with no answer in any document:** who owns the safe-return policy for every hookable TMM function, *in perpetuity*, and what mechanical gate catches it when someone edits one of those functions two releases from now? Skipping a body is a per-function proof obligation over buffer ownership, refcounts, locks held on entry, flow-state advancement, and divergence from the mirrored connflow. There is no test for the absence of an obligation you didn't think of. The failure mode: a shield blocks the CVE, passes red team, ships, and leaks a packet buffer on one customer's traffic mix three weeks later — presenting as a memory-pressure TMM restart with no attribution back to the shield.

**The answer that was offered, and that reframes the proposal:** v1 enforce-capable boundaries are limited to an explicitly annotated allow-list of leaf functions that own no buffer, hold no lock and advance no flow state — with a CI check that fails the build if a listed function gains a callee, a lock, or a non-trivial return — and **everything else is observe-only, or uses a designed-in call site where the drop branch already exists in TMOS source.** That is a smaller product than the README describes, and it is the one this read could not find an argument against.

---

## 8 · The recommended path

**Three things to prove in a lab**, on top of §7's two paper decisions. Each is small next to building
the engine, and they are ordered so the cheapest possible *no* comes first: **(1)** asks whether the
mechanism is affordable at all, **(2)** whether anything useful can be expressed, and **(3)** whether the
pieces compose without wrecking serviceability. That ordering is the point — a failure at (1) means the
rest was never worth designing.

1. **The cost of the compiler flag, measured twice — the experiment itself is specified in §4.** The two
   numbers answer different questions. The **idle cost, nothing armed**, is the only cost every customer
   pays whether or not a shield is ever loaded, and the pads make it second-order: text growth,
   instruction-cache locality, i-TLB pressure, and perturbation of the optimiser's layout and inlining
   decisions, which can move performance in *either* direction. The **armed cost, at rate** is what
   decides whether per-packet shielding is available at all (`engine-hard-problems.md` §1.1), and it is
   cheap once the first build exists — same rig, one hook armed.
2. **A `ctx` model that actually verifies, against real TMM debug info.** Two independent difficulties meet here. PREVAIL's side: there is no `--program-type`, the context descriptor is four integers with no read-only region (so the program can write its own ctx, forcing the per-core copy), and a load out of `ctx` yields an unconstrained *number* — dereferencing which is refused however many NULL checks precede it, so the program can never chase a pointer and must receive resolved scalars. TMM's side: the host therefore walks the pointers in native C, and that walker is **generated per build from DWARF**, which means classifying struct-by-value splitting, HFA/HVA, stack spill and unions correctly against an *optimised* build. The test: reuse PREVAIL's `tracing` program type, copy the ctx into per-core scratch, and express the worked CVE as a scalar predicate whose field offsets are demonstrably right for that build. If the CVE class we care about *needs* pointer chasing, then day one includes a bounded `probe_read` helper and the §4 threat surface grows — decide that now, on paper, with SIRT in the room.
3. **One hook armed end-to-end on one architecture in a lab TMM**, with core dumps and backtraces still working through the trampoline. The debuggability clause is not an afterthought and belongs in the same sentence: a trampoline that *works* while destroying post-mortem analysis is unshippable in a product whose support model runs on core dumps and qkviews (§3). It needs correct CFI or every backtrace through a hooked function is garbage; a PC inside JIT'd code must be resolvable; the dump must record which shields were armed, or every subsequent crash on that box is ambiguous. There is also a hard fail lurking — jumping to `entry + pad_bytes` is an indirect branch into mid-function, which faults immediately on CET-IBT/BTI-hardened builds without a landing pad. Note the scope: **one** hook, **one** architecture, a lab. A minimal composition proof, not a product.

If §7's two decisions and these three all land, what follows is a subsystem-scale programme (§5), scoped as §5 describes rather than as `development-scope.md` did. If any of the three fails, it fails before the subsystem is built rather than in month nine.

---

*A review register. Findings are recorded here; fixes land in the documents they affect and are marked in this table as they do.*
